{
  "name": "twistea",
  "version": "1.0.0",
  "description": "Twistea - Indian Tea E-Commerce Platform",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "seed": "cd backend && node seed.js",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}
